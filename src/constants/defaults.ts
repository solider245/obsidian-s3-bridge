export const TIMEOUTS = {
	PRESIGN_DEFAULT: 10000,
	UPLOAD_DEFAULT: 25000,
	PRESIGN_MIN: 1000,
	UPLOAD_MIN: 1000,
}

export const UPLOAD = {
	MULTIPART_THRESHOLD: 10 * 1024 * 1024,
	MAX_CONCURRENT_UPLOADS: 3,
	MAX_RETRIES: 3,
	RETRY_DELAY_MS: 1000,
	PRESIGN_EXPIRY_SECONDS: 3600,
	DEFAULT_EXPIRY_SECONDS: 300,
}

export const SYNC = {
	AUTO_SYNC_INTERVAL_MS: 30 * 60 * 1000,
	MAX_SYNC_RETRIES: 3,
}

export const NOTIFICATION = {
	DURATION_SHORT: 2000,
	DURATION_NORMAL: 3000,
	DURATION_LONG: 5000,
	DURATION_PERMANENT: 0,
}

export const LOG_CLEANUP = {
	DAYS_TO_KEEP: 30,
}

export const FILE_SIZE = {
	LARGE_FILE_THRESHOLD: 5 * 1024 * 1024,
}
